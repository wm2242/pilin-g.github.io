这是一篇专门为**测试自建图床**而写的 MDX 博客。所有图片链接使用了占位符，你可以替换为自己图床的真实直链，逐一验证各项功能。

```mdx
---
title: '自建图床深度测试：从零到一条龙验证'
date: '2026-07-19'
lastmod: '2026-07-19'
tags: ['图床', 'Telegram', 'Cloudflare', 'Sanyue', 'test']
draft: false
summary: '搭建完基于 Telegram + Cloudflare Workers 的私人图床后，我进行了一系列严苛测试，包括多格式图片、GIF、视频、尺寸控制、图片链接等，此文记录测试结果。'
layout: PostLayout
---

## 为什么要自建图床

市面上的免费图床要么不稳定，要么悄悄给图片加水印、压缩画质，甚至删除“违规”图片。于是我用 **Sanyue** 部署了一套完全由自己控制的图床：图片存于我的私密 Telegram 频道，通过 Cloudflare Workers 代理，再绑定个人域名。

现在图床搭建完成，我写了一篇测试文章来验证它的各项能力。如果你也刚搭好图床，可以跟着这篇文档逐一检测。

> ⚠️ 为方便测试，下文所有图片链接都使用了占位符，请将其替换为你图床上传的真实直链。

---

## 测试 1：基础图片渲染

**单张 JPG 图片，带 alt 文本**  
```
![一张测试配图](https://img.guoblog.dpdns.org/file/1784377866787_huang-lun.png)
```
预期效果：图片正常显示，不会出现裂图；鼠标悬停可以看到 alt 提示文字。

![一张测试配图](https://img.guoblog.dpdns.org/file/1784396108034_am1.png)

---

## 测试 2：不同图片格式

图床应该支持常见格式。我分别上传了 PNG、WebP 和 GIF，以验证透明度和动图是否正常。

**PNG 透明图**  
```
![PNG透明图标](https://你的图床域名/file/你的图片fileId2)
```
![PNG透明图标](https://你的图床域名/file/你的图片fileId2)

**WebP 图片（体积更小）**  
```
![WebP皇汉旗](https://img.guoblog.dpdns.org/file/1784396176208_Flag_of_Han_Nation__original_.svg.webp)
```
![WebP风景照](https://你的图床域名/file/你的图片fileId3)

**GIF 动图**  
```
![GIF动图测试](https://img.guoblog.dpdns.org/file/1784396889241_gif.gif)
```
![GIF动图测试](https://img.guoblog.dpdns.org/file/1784396889241_gif.gif)

如果上面三张图都正常显示，且 GIF 会动，说明图床对不同格式的支持已经通过。
PNG后面补

---

## 测试 3：图片作为链接

图床图片不仅可以插入，还能作为可点击的链接。例如点击下方图片可以跳转到我的博客首页。

```markdown
[![点击访问图床](https://img.guoblog.dpdns.org/file/1784396388270_屏幕截图_2025-11-21_183217.png)](https://img.guoblog.dpdns.org/)
```
[![点击访问博客](https://img.guoblog.dpdns.org/file/1784396388270_屏幕截图_2025-11-21_183217.png)](https://web.guoblog.dpdns.org/)

预期：图片正常加载，点击后打开新页面。

---

## 测试 4：控制图片尺寸

Markdown 原生不支持设置图片尺寸，但我可以在 MDX 中混写 HTML 来实现缩放，而不会影响图床链接。

```html
<img src="https://img.guoblog.dpdns.org/file/1784396470450_a.png" width="300" alt="缩小后的图片" />
```
<img src="https://img.guoblog.dpdns.org/file/1784396470450_a.png" width="300" alt="缩小后的图片" />

如果图片以 300 像素宽度显示，说明 HTML 标签正常解析，并且图床支持任意尺寸的调用。

---

## 测试 5：多图并排展示

有时候需要在博客中展示多张截图对比，我使用 HTML 表格来实现并排图片。

```html
<table>
  <tr>
    <td><img src="https://你的图床域名/file/你的图片fileId7" width="200" /></td>
    <td><img src="https://你的图床域名/file/你的图片fileId8" width="200" /></td>
  </tr>
</table>
```
<table>
  <tr>
    <td><img src="https://你的图床域名/file/你的图片fileId7" width="200" /></td>
    <td><img src="https://你的图床域名/file/你的图片fileId8" width="200" /></td>
  </tr>
</table>

两图应水平并排，且各自清晰。
后补

---

## 测试 6：MP4 短视频（可选）

Sanyue 图床其实也支持小视频（Telegraph 接口限制 5MB，Bot 频道 20MB）。我上传了一段 3 秒的演示视频来测试。

```html
<video width="400" controls>
  <source src="https://你的图床域名/file/你的视频fileId" type="video/mp4">
  你的浏览器不支持视频标签。
</video>
```
<video width="400" controls>
  <source src="https://img.guoblog.dpdns.org/file/1784397029390_1216946090_cd493104d10a44d48206cd98aa54956f_sd.mp4" type="video/mp4">
  你的浏览器不支持视频标签。
</video>

如果视频可以播放，图床的多媒体能力就更全面了。

---

## 测试 7：管理能力——删除与失效

这张图片是我临时上传的，随后我会在 Telegram 频道中**删除这条消息**。

![即将被删除的图片](https://你的图床域名/file/你的图片fileId9)

删除后再刷新博客，上方应该出现一张裂图（或者直接不显示）。这证明我对图床拥有完全控制权，能够随时移除不当内容。
后面再测
---

## 测试结论

经过以上七项测试，我的自建图床表现如下：

| 测试项目 | 结果 | 备注 |
|---------|------|------|
| 基础 JPG 显示 | ✅ | 正常 |
| PNG 透明度 | ✅ | 透明背景无白底 |
| WebP 格式 | ✅ | 体积小，画质佳 |
| GIF 动图 | ✅ | 动画循环正常 |
| 图片链接 | ✅ | 点击跳转正确 |
| HTML 控制尺寸 | ✅ | 缩放无锯齿 |
| 多图并排 | ✅ | 布局正常 |
| 视频播放 | ✅ | 加载稍慢但可播 |
| 删除管理 | ✅ | 图片立即可控 |

全部通过，图床搭建宣告成功。如果你也想拥有这样一个免费、私密、高度可控的图床，不妨参考我之前的搭建记录（这里可以放内链）。

## 最后的建议

1. **本地备份**：即使图床再稳，也记得在本地保存一份原图。
2. **域名续费**：如果是自己买的顶级域名，设置好自动续费，避免过期导致图片全挂。
3. **监控提醒**：偶尔打开博客检查图片加载情况，有问题及时修复。

现在，我的每一篇博文配图都有了安心的家。希望你也能享受这份踏实感。
```

---

使用方式：
1. 将内容保存为 'mdx'。
2. 在你的图床上传各种测试图片和视频，获取对应的直链。
3. 用真实的直链替换文中所有 `https://你的图床域名/file/...` 占位符。
4. 发布到博客，按照测试项逐一验收。这样就能系统化地验证你图床的全部能力，而且博客本身也成了一篇实用的技术记录。
